#Scatter plot. To work on later.
#This scatterplot would be used to find if there are large clumps of data. For example, which specific age group would be most susecpible to Lung cancer.
#FYI, <- is an = sign in R. == expresses equivilance, while <- is like storing a value into an integer.
dattbl <- data.csv

plot(
  "Probability of Developing Cancer (%) by Age","Risk"
  col=”darkblue”,
  pch=20,
  main="The Probability of Developing Cancer by Age",
  xlab="Probability of Developing Cancer by Age",
  ylab="Risk"
)
